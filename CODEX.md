# Punto de entrada para Codex

Lee **[`AGENTS.md`](AGENTS.md)** en la raíz antes de modificar nada: es la guía
multi-proveedor de las invariantes de este repo (compuerta de validación + Gitleaks,
contrato público de endpoints y snippets prohibidos, fragilidad de firma/XML,
corrección del CUFE, semántica de status HTTP, sin secretos ni branding privado).
`CLAUDE.md` y este archivo solo apuntan allí.

Para **integrar** la API, usa la skill `dian-integration` (`.agents/skills/dian-integration`).
